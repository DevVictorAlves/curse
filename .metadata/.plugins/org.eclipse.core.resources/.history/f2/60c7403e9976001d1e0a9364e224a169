package streams;

public class DesafioMap {

	public static void main(String[] args) {
		
		Utilitarios.nums.stream()
		.map(Utilitarios
		.IntegerPString)
		.map(Utilitarios
		.converteString)
		.map(Utilitarios.converteParaInt)
		.forEach(Utilitarios.print);
		

	}
}
